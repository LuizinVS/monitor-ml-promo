spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mlpromo
    username: mlpromo
    password: mlpromo

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

mercadolivre:
  client-id: ${ML_CLIENT_ID}
  client-secret: ${ML_CLIENT_SECRET}
  redirect-uri: ${ML_REDIRECT_URI}